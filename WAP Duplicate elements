#include <stdio.h>

     
int main()    
{    
int size=7;
int arr[size]; 
    int temp = 0; 
    
for(int i=0;i<size;i++){
   scanf("%d",&arr[i]);
} 
                
    //Sort the array in ascending order    
    for (int i = 0; i < size; i++) {     
        for (int j = i+1; j < size; j++) {     
           if(arr[i] == arr[j]) {    
            printf("%d\n", arr[i]);
           }     
        }     
    }    
    
    
    return 0;    
}  
